import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {EditorialArchive} from '@/components/editorial-archive';import {getCategoryArchive} from '@/lib/editorial-content';export const dynamic='force-dynamic';export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const {category}=await getCategoryArchive(slug);const n=category?.name||slug;return {title:`${n} Juve: news, analisi e approfondimenti`,description:`Archivio ${n} di BianconeriHub con notizie Juventus, analisi e approfondimenti della redazione.`}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=await getCategoryArchive(slug);if(!x.category&&!x.articles.length)notFound();return <EditorialArchive kind="Categoria" title={x.category?.name||slug} articles={x.articles}/>}
