import { getSeoArticles,xml } from '@/lib/public-seo'
export async function GET(){const site=(process.env.NEXT_PUBLIC_SITE_URL||'https://bianconerihub.com').replace(/\/$/,'');const articles=await getSeoArticles();const items=articles.slice(0,50).map(a=>`<item><title>${xml(a.title)}</title><link>${site}/articolo/${xml(a.slug)}</link><guid>${site}/articolo/${xml(a.slug)}</guid><pubDate>${new Date(a.published_at).toUTCString()}</pubDate><description>${xml(a.excerpt)}</description></item>`).join('');const body=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>BianconeriHub</title><link>${site}</link><description>Il blog indipendente dedicato alla Juventus.</description><language>it-IT</language>${items}</channel></rss>`;return new Response(body,{headers:{'Content-Type':'application/rss+xml; charset=utf-8','Cache-Control':'public, max-age=300, s-maxage=900'}})}
