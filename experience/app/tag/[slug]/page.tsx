import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {EditorialArchive} from '@/components/editorial-archive';import {getTagArchive} from '@/lib/editorial-content';export const dynamic='force-dynamic';export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const {tag}=await getTagArchive(slug);const n=tag?.name||slug;return {title:`${n} Juve: archivio topic e articoli correlati`,description:`Archivio tag ${n} di BianconeriHub con contenuti, analisi e articoli dedicati al mondo Juventus.`}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=await getTagArchive(slug);if(!x.tag&&!x.articles.length)notFound();return <EditorialArchive kind="Tag" title={`#${x.tag?.name||slug}`} articles={x.articles}/>}
